# mcp 2.0 переименовал FastMCP в MCPServer и убрал mcp.server.fastmcp,
# из-за чего main.py падает на импорте. Держим 1.x до осознанной миграции.
mcp>=1.0.0,<2
uvicorn[standard]>=0.27.0
starlette>=0.36.0
httpx>=0.27.0
pydantic>=2.8.0
weasyprint>=60
jinja2>=3
# Текст из PDF-вложений (get_attachment_text). Чистый Python, без системных
# библиотек — на сборку образа Railway не влияет.
pypdf>=5
# Распознавание сканов: Pillow достаёт картинки страниц из PDF, pytesseract
# зовёт системный tesseract (см. tesseract-ocr в nixpacks.toml / Dockerfile).
pillow>=10
pytesseract>=0.3.10
